export const Shoe = function(colorr, titlee, pricee, imagee,keyy) {
        const key = keyy;
        const color = colorr;
        const title = titlee;
        const price = pricee;
        const image = imagee;
    return { color, title, price, image, key };
    }
   
